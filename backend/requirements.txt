# TailorTom Backend Dependencies
dspy>=3.0.0b4
openai>=1.0
PyPDF2>=3.0
pymupdf>=1.24  # For PDF annotation and text extraction
psutil>=5.9.0  # For memory usage tracking
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
jupyter
ipywidgets
pdf2image>=1.16  # For visual PDF comparison (requires poppler: brew install poppler)

# FastAPI dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Celery and Redis dependencies
celery>=5.3.0
redis>=5.0.0
kombu>=5.3.0

# DOCX conversion
python-docx>=1.0

# Database (PostgreSQL + ORM + migrations)
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# Auth (Google OAuth + JWT)
pyjwt[crypto]>=2.8
httpx>=0.27
